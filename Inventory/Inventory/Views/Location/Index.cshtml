@model IEnumerable<Inventory.Models.Tbl_Location>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lo_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Lo_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Lo_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Lo_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Lo_ID })
        </td>
    </tr>
}

</table>
